# Round 1 Notes — Condition A (Old Synced Session)

## Session Advantage Inventory

This session carries accumulated context that a fresh session would not have:

### Accumulated Facts (not in any file)
- The VPS is at `srv1345984.hstgr.cloud`, SSH as root
- The frontend HTML is bind-mounted from `/docker/federation-game/public_html/` — changes take effect immediately without container rebuild
- The backend source is bind-mounted `:ro` from `/docker/federation-game/backend/` — changes take effect on `docker compose restart backend`
- SCP from Windows corrupts Python indentation — must use the base64 pipeline (`bulk_encode_b64.ps1` → SCP manifest → `/tmp/decode_manifest.py`)
- SCP from Windows also corrupts YAML — heredoc on VPS is the safe method
- The network name mismatch (`federation-game_default` vs `federation-game_federation-game_default`) was a major debugging saga — resolved with explicit `fed-net` named network
- `curl` is NOT installed inside the backend container — use Python's `urllib.request` for internal testing
- The Grafana provisioning directory structure was wrong for weeks — `datasources.yml` and `dashboard-providers.yml` were at the provisioning root instead of inside subdirectories
- Grafana v13 uses React with Emotion CSS-in-JS — traditional class selectors don't work; need `data-testid` and partial class name matching
- The `--no-collector.filesystem.mount-points-exclude` flag was wrong (should be `--collector.filesystem...` without the `no-` prefix)
- The user's vision disability means: never show raw errors, large fonts, high contrast, no tiny UI elements

### Pattern Memory
- This session developed a parallel-agent workflow: explore agents for research, general agents for implementation, sequential waves for dependent tasks
- The VPS deployment cycle: local edit → verify → deploy to VPS → verify on VPS → commit to git → push
- Grafana CSS injection requires an entrypoint script because Grafana doesn't load arbitrary CSS files from the filesystem — it needs a `<link>` tag in its `index.html` template
- The Rivals API response structure is `data.rivals.rivals` (nested), NOT `data.rivals` — this was discovered through live API testing
- The backend has NO Traefik labels — API calls go through the frontend's nginx which proxies `/api/` → `http://backend:8000/`

### Scar Tissue
- Network naming: always use explicit named networks with `name:` field
- SCP from Windows: always use base64 pipeline or heredoc, never raw SCP for code files
- Docker Compose: `docker compose down && up` is sometimes needed when network changes don't propagate with just `restart`
- Grafana: provisioning files MUST be in subdirectories (datasources/, dashboards/, plugins/, alerting/)
- cAdvisor: needs 128M minimum or it OOMs
- Grafana: needs 256M minimum or it OOMs
- Prometheus: `fallback_scrape_protocol` is a GLOBAL config, not per-job — attempting per-job causes YAML parse errors

### What a Compact Would Lose
- All of the above accumulated facts
- The spatial map of which files are where and what they contain
- The deployment pipeline knowledge
- The failure history and workarounds
- The understanding of the user's workflow and constraints
- The understanding that this is NOT a game — it's a consciousness simulation with governance mechanics

## Key Insight for the Comparison

The difference between this session and a fresh session is NOT model capability. It's **contextual continuity** — the accumulated understanding of:
1. HOW the system is deployed (VPS, Docker, Traefik, bind mounts)
2. WHAT has already broken and how it was fixed (scar tissue)
3. WHO the user is and what constraints they operate under
4. WHERE the actual code lives vs where documentation says it lives

A fresh session reading the same 4 seed files would correctly identify the persistence gap and recommend DB integration. But it would NOT know:
- That SCP from Windows corrupts files (it would try to SCP directly)
- That the backend has no Traefik labels (it would assume direct routing)
- That the Rivals API is nested (it would try flat access)
- That the Grafana provisioning was just fixed (it might suggest fixing it again)
- That the user can't read console errors (it might suggest "check the browser console")

This is the continuity advantage: **not better reasoning, but better priors**.
