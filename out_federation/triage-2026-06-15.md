# Dirty-Tree Triage Report
**Generated:** 2026-06-15
**Working tree:** `S:\federation`
**Branch:** main, dirty count: 26
**Last git release:** 8265504 (codex's P010 — NPC chat improvements recovered from VPS)
**All 26 dirty files share mtime 2026-06-14 04:05:04 UTC** — likely workspace-checkout touching, but every diff is *real* (additions / deletions / replacements), not noise.

## Frame

Three buckets, in priority of risk:
- **KEEP** — deployed to VPS already (or in-flight shape matches deployed shape). Discard nothing, don't ship to VPS.
- **RECOVER** — local-dirty contains work that *might* not be on the VPS. Need to verify before either keep or discard.
- **DISCARD** — local-dirty is superseded, redundant, or wrong.
- **CODEX-BLOCK** — too complex or too risky to decide without a second pair of eyes / earlier-work context the user/agent had.

## Status code, pre-flight

There is **Postgres-state anomaly** flagged by codex in its last session: the `public` schema in Postgres looks "effectively empty" on direct query, but `/npc-turns/analyze` still returns fleet data. Until that is solved, **no `simulation_operator.py`-flavored write path should be exercised**. This means several "RECOVER" candidates below should be paused until the data-routing mystery is closed.

There is **downgraded risk** on `simulation_operator.py` and `routes/simulation.py` (RESOLVED 2026-06-15 by Hermes): production backend on VPS still routes `/simulation/operator/tick` through `tick_engine`, not `simulation_operator`. The dirty diff refactors an endpoint that has hit count = 0 in last hour of backend logs. Operator module is loaded but only the `/operator/{status,tick}` endpoints touch it — other endpoints use legacy paths. **If the dirty `routes/simulation.py` is never deployed to VPS, user-visible behavior does not change.** Still CODEX-BLOCK pending their file-content read, but the "currently-breaking-the-server" hypothesis is weakened.

## Triage table

Ranked by hunks/line counts (more lines = bigger cognitive surface = needs more attention).

### Tier 1 — Big files, sensitive (CODEX-BLOCK pending state mystery)

| File | Hunks +/- | Verdict | Notes |
|---|---|---|---|
| `frontend/starmap.js` | 2720 / 2706 | CODEX-BLOCK | Massive rewrite in same shape — most lines are mass-deletion then mass-replacement at file start. Almost certainly a feature build. Cannot verify without context. |
| `backend/federation_game_db.py` | 803 / 399 | RECOVER (DELICATE) | Persistence layer rewrite. Codex flagged Postgres-state anomaly at end of last session. **Block until public-schema question answered.** |
| `frontend/npc-logs.html` | 148 / 4 | RECOVER | New NPC-logs UI (139 net additions, 4 deletions). Tightly coupled with `routes/npc_logs.py` (also dirty at 104/12) and `npc_activity_logger.py` (60/0). Three-files-as-one feature. |
| `backend/simulation_operator.py` | 126 / 5 | CODEX-BLOCK | Largest single feature in dirty tree: introduces YAML config load, apprise notification, recovery keys, TTL. Triggers public-schema-aware writes. **Block.** |
| `backend/routes/npc_logs.py` | 104 / 12 | RECOVER (paired with html + logger) | New NPC-logs endpoint. |
| `frontend/spectator.html` | 89 / 42 | RECOVER | Spectator UI rewrite, likely tied to `simulation_operator.py`. Paired. |
| `frontend/spectator.js` | 67 / 56 | RECOVER | Spectator JS — paired with spectator.html. |

### Tier 2 — Mid-size, likely feature additions

| File | Hunks +/- | Verdict | Notes |
|---|---|---|---|
| `backend/npc_activity_logger.py` | 60 / 1 | RECOVER (with npc-logs trio) | Plain log of NPC activity — paired with the npc-logs feature. |
| `frontend/nginx-default.conf` | 60 / 0 | RECOVER | Adds `location /choose`, `/reset`, `/systems-overview` proxy blocks. Required for any backend routes with those prefixes to actually be served by the frontend Nginx. |
| `session/bridge/bridge_state.json` | 15 / 3 | DISCARD (review-time) | Runtime state, not source. Should never be in git. **Re-add to `.gitignore`.** Add to long-tail: `git checkout -- session/bridge/bridge_state.json` after session end. |
| `frontend/constellation.html` | 14 / 10 | RECOVER | HTML UI change. |
| `.kilo/kilo.json` | 13 / 0 | DISCARD | Kilo agent config (uncommitted changes). Local-only. |
| `frontend/constellation.js` | 9 / 4 | RECOVER | Constellation JS — paired with constellation.html. |
| `docker-compose-vps.yml` | 8 / 4 | DISCARD-LIKELY (RESOLVED 2026-06-15) | Adds `NVIDIA_API_KEY` env pass-through + Traefik admin-router expansion. Sean's NIM rotation is not a bottleneck (20B personal credits/mo + Ollama local + OpenRouter free). NVIDIA half is forward-compat only. Traefik half needs separate verification. |

### Tier 3 — Author did _something_ on these but the diffs are tiny

| File | Hunks +/- | Verdict | Notes |
|---|---|---|---|
| `backend/routes/simulation.py` | 7 / 9 | CODEX-BLOCK | Refactors `/simulation/operator/tick` from `tick_engine` into `simulation_operator`. Triggers operator path (state-mystery risk). |
| `frontend/index.html` | 4 / 0 | DISCARD (likely) | Likely incidental whitespace / single line. Tied to nothing obvious. |
| `backend/nvidia_nim_client.py` | 4 / 0 | DISCARD | Adds `NVIDIA_API_KEY` env acceptance. Forward-compat only — `NIM_API_KEY_1..6` rotation is not a bottleneck. **Verify by checking whether the env var is set on VPS backend container — ANSWERED: no, so the change never fires; safe to discard.** |
| `frontend/starmap.css` | 2 / 2 | DISCARD (likely) | Cosmetic. |
| `frontend/simulation.html` | 2 / 1 | RECOVER | Two-line change; probably a link or title. |
| `backend/map_endpoints.py` | 1 / 1 | DISCARD (likely) | Single-line. EOL or import order. |
| `backend/main.py` | 1 / 1 | DISCARD (likely) | Router ordering tweak. Verify by reproducing the order logic against current codex-dirty files. |
| `backend/federation_game_npc_integration.py` | 1 / 1 | DISCARD (likely) | Single-line. |
| `frontend/worldguide.html` | 1 / 0 | DISCARD | Single-line. EOL. |
| `frontend/starmap.html` | 1 / 0 | DISCARD | Single-line. EOL. |
| `frontend/earth.html` | 1 / 0 | DISCARD | Single-line. EOL. |
| `frontend/bridge.html` | 1 / 0 | DISCARD | Single-line. EOL. |

## Summary by verdict

- **DISCARD (likely noise):** 13 files (mostly 1-line noise + kilo/state/integration noise + the two NVIDIA forward-compat files)
- **RECOVER:** 8 files (feature additions tied tightly together)
- **CODEX-BLOCK:** 4 files (need expert re-read because of stakes or surface area)
- **RECOVER (delicate, hold):** 1 file (`federation_game_db.py` — blocked on Postgres-state mystery)
- **`session/bridge/bridge_state.json`** — runtime state, should never be in git. Add `.gitignore` entry.

## Order of operations

1. **(NOW, me)** Housekeeping first: `.gitignore` for `session/bridge/bridge_state.json`. Then `git checkout -- .kilo/kilo.json`. Both reversible. Don't touch the rest.
2. **(REST, me solo)** Build a "recovered, ready for review" patch: stage + commit just the RECOVER + DISCARD-likely files in two commits so user can revert atomically. Or hold off if user wants Phase 2 width-first.
3. **(CODEX, with prompt)** Hand codex a focused prompt: "Review these 4 files for correctness vs the visible feature intent: starmap.js, simulation_operator.py, federation_game_db.py, routes/simulation.py. Also flag anything in the discard-likely tier that looks suspicious. Don't act, just review."
4. **(USER)** Sign off. Then merge to main. Stay free of VPS deploys until Postgres-state question closes.

## Big questions still unanswered

- **Postgres public-schema anomaly.** Without solving this, no RECOVER commit of `simulation_operator.py` or `federation_game_db.py` should ship.
- **Whether `nvidia_nim_client.py`'s `NVIDIA_API_KEY` change is redundant or load-bearing on the live deploy.** **RESOLVED 2026-06-15 by Hermes (DISCARD):** VPS `.env` has only `NIM_API_KEY_1..6`. No `NVIDIA_API_KEY` in production `.env` or container env. Sean runs 20B tokens/mo personal credits plus Ollama local + OpenRouter free — NIM rotation is not a bottleneck. Change is forward-compat, ships at zero value today. → discard.
- **`docker-compose-vps.yml` Traefik rule expansion.** Need to see the actual added path-prefixes from the diff. The truncation in codex's last export hid them. Worth a fresh look. Sean noted: the production yaml at line 30+ on VPS has only `env_file: - .env` and no `environment:` block. Whatever Traefik paths the diff adds are not on production. If they're meant to expose new admin UI routes, those routes will 404 until this is shipped. **Still needs codex read.**
